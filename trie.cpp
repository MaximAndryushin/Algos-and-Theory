#include<bits/stdc++.h>
 
#define endl "\n"
using namespace std;
using ll = long long;
using ld = long double;
using pii = pair<int, int>;
 
constexpr int N = 5e5 + 5;
 
/*
БОР

хотим найти все вхождения строки в множество

Бор - запись  мн-ва строк в виде дерева

ссылка типа префикс-функции

ссылка на терминальную вершину

сложность (суммарная длина строк + работа со строкой T + кол-во откатов)

Битовый бор

даны числа a
надо максимизировать аi^aj спускаемся по бору идем в поддервево ^a[i][j]
кол-во пар ai^aj >= k

ахо-карасик
есть мн-во слов
хотим построить слово, которое не содержит подстрок борных
*/
 
void Solve(){
  
}
 
int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  Solve();
  return 0;
}